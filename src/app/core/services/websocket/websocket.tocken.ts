import { InjectionToken } from '@angular/core';

export const WEBSOCKET_URL = new InjectionToken<string>(
  'WebSocket Injection Token'
);

export const WEBSOCKET_CONFIG = new InjectionToken('WebSocket Injection Token');
